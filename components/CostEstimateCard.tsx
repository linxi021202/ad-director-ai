import type { CostEstimate, CostModeEstimate } from "@/lib/schemas/project";

type CostEstimateCardProps = { estimate?: CostEstimate; costEstimates?: CostModeEstimate[]; compact?: boolean };

export function CostEstimateCard(_props: CostEstimateCardProps) {
  return null;
}
